accelerate>=0.34.0
arq>=0.28.0
boto3>=1.34.0
datasets==5.0.1
evaluate>=0.4.3
mlflow>=3.0.0
pydantic-settings>=2.14.2
python-dotenv>=1.2.2
redis>=5.3.1
# s3fs ผูก fsspec แบบ exact-match กับตัวเอง (s3fs==X ต้องคู่กับ fsspec==X) และ datasets/evaluate
# ก็ pin fsspec ของตัวเองไว้อีกที ถ้าปล่อย s3fs แบบไม่ pin จะได้ตัวใหม่สุดที่ fsspec ไม่ตรงกับ
# ที่ datasets ต้องการ resolve ไม่ได้ (ยืนยันจาก datasets/s3fs ที่ resolve ผ่านจริงใน backend/)
s3fs==2026.6.0
seqeval>=1.2.2
transformers>=4.45.0
